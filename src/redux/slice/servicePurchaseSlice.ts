import { createSlice } from '@reduxjs/toolkit';
const servicePurchaseSlice = createSlice({
  name: 'servicePurchase',
  initialState: { data: [], loading: false },
  reducers: {},
});
export default servicePurchaseSlice.reducer;
